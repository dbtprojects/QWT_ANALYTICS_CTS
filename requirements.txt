# Astronomer Cosmos - run dbt in Airflow
astronomer-cosmos[dbt-all]>=1.3.0

# dbt adapter for your data warehouse (add the specific one you're using)
# Uncomment the one you need:
dbt-snowflake>=1.5.0
# dbt-bigquery>=1.5.0
# dbt-redshift>=1.5.0
# dbt-postgres>=1.5.0
